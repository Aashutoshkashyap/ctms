import React from 'react';
export default function RecordDetailsDialog({ title, record, onClose }: { title: string; record: object | null; onClose: () => void }) { if (!record) return null; return <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/55 p-4"><section className="max-h-[85vh] w-full max-w-2xl overflow-auto rounded-2xl bg-white p-5 shadow-2xl"><div className="flex justify-between gap-4"><div><h2 className="text-lg font-extrabold text-slate-950">{title} details</h2><p className="text-sm text-slate-600">Full record information for the active project.</p></div><button onClick={onClose} className="rounded-lg border border-slate-300 px-3 py-1.5 font-bold text-slate-800">Close</button></div><dl className="mt-5 grid gap-3 sm:grid-cols-2">{Object.entries(record).filter(([,value])=>value!==undefined&&value!==null&&value!=='').map(([key,value])=><div key={key} className="rounded-lg border border-slate-200 bg-slate-50 p-3"><dt className="text-xs font-bold uppercase text-slate-600">{key.replaceAll('_',' ')}</dt><dd className="mt-1 break-words text-sm text-slate-950">{typeof value === 'object' ? JSON.stringify(value) : String(value)}</dd></div>)}</dl></section></div>; }
